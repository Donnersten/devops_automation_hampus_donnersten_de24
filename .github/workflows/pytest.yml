
name: pytest


on:
   workflow_run:
      workflows: [more_jobs]
      types:
         - completed
         

env:
 APP_NAME: pytest
 GREETINGS: "Hello, 1+1 is 2"

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Install pytest
        run: pip install pytest

      - name: Run tests
        run: |
          echo "$GREETINGS"
          pytest -v

  build:
    runs-on: ubuntu-latest
    needs: test 
    if: success() 
    steps:
      - uses: actions/checkout@v4
      - name: Build application
        run: |
          echo "Alla tester är klara"
          echo "Bygger $APP_NAME..."
